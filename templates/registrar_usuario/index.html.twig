{% extends 'base.html.twig' %}

{% block title %} RegistrarUsuarioController!{% endblock %}

{% block body %}
    {% for message in app.flashes('exito') %}
        <div class="alert alert-success">
            {{message}}
        </div>
    {% endfor %}
    {{form(formularioUsuario)}}
{% endblock %}
