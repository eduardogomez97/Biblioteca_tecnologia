{% extends 'base.html.twig' %}

{% block title %}Inicio{% endblock %}

{% block body %}
{#<div class="count">
    {{ pagination.getTotalItemCount }}
</div>
<table>
    <tr>
        
        <th>{{ knp_pagination_sortable(pagination, 'Id', 'a.id') }}</th>
        <th{% if pagination.isSorted('a.Nombre') %} class="sorted"{% endif %}>
            {{ knp_pagination_sortable(pagination, 'Nombre', 'a.Nombre') }}
        </th>
        <th{% if pagination.isSorted('a.num_trabajadores') %} class="sorted"{% endif %}>
            {{ knp_pagination_sortable(pagination, 'Nº Trabajadores', 'a.num_trabajadores') }}
        </th>
        <th{% if pagination.isSorted('a.direccion') %} class="sorted"{% endif %}>
            {{ knp_pagination_sortable(pagination, 'Dirección', 'a.direccion') }}
        </th>
        <th{% if pagination.isSorted('a.FechaFundacion') %} class="sorted"{% endif %}>
            {{ knp_pagination_sortable(pagination, 'Fecha de fundación', 'a.FechaFundacion') }}
        </th>
        
    </tr>

  
    {% for article in pagination %}
        <tr {% if loop.index is odd %}class="color"{% endif %}>
            <td>{{ article.id }}</td>
            <td>{{ article.nombre}}</td>
            <td>{{ article.num_trabajadores}}</td>
            <td>{{ article.direccion}}</td>
            <td>{{ article.fecha_fundacion | date('Y-m-d') }}</td>
        </tr>
    {% endfor %}
</table>

<div class="navigation">
    {{ knp_pagination_render(pagination) }}
</div>#}

<div class="container mt-5">
    <div class="row">
        <div class="col">
            <table id="bibliotecas" class="table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>NOMBRE</th>
                        <th>Nº TRABAJADORES</th>
                        <th>DIRECCION</th>
                        <th>FECHA DE FUNDACIÓN</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for article in pagination  %}
                        <tr {% if loop.index is odd %}class="color"{% endif %}>
                            <td>{{ article.id }}</td>
                            <td>{{ article.nombre}}</td>
                            <td>{{ article.num_trabajadores}}</td>
                            <td>{{ article.direccion}}</td>
                            <td>{{ article.fecha_fundacion | date('d-m-Y') }}</td>
                            <td><a href="verBiblioteca/{{ article.id }}"><img src="/img/look.png" /></a> <a href="editarBiblioteca/{{ article.id }}"><img src="/img/editar.png" /></a> <a href="borrarBiblioteca/{{ article.id }}"><img src="/img/delete.png" /></a></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
